@model List<AgriEcommerces_MVC.Models.user>

@{
    ViewData["Title"] = "Quản lý người Bán";
}

<div class="row mb-4 align-items-center">
    <div class="col">
        <h2 class="mb-0"><i class="fas fa-users me-2"></i>@ViewData["Title"]</h2>
    </div>
    <div class="col-auto">
        <a asp-area="Management"
           asp-controller="ManagerFarmer"
           asp-action="Create"
           class="btn btn-primary">
            <i class="fas fa-plus me-2"></i>Thêm người bán
        </a>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th scope="col">Tên người bán</th>
                    <th scope="col">Email</th>
                    <th scope="col">Vai trò</th>
                    <th scope="col">Ngày tạo</th>
                    <th scope="col">Hành động</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    @foreach (var user in Model)
                    {
                        <tr>
                            <td>@user.fullname</td>
                            <td>@user.email</td>
                            <td>Người bán hàng</td>
                            <td>@user.createdat?.ToString("dd/MM/yyyy HH:mm")</td>
                            <td>
                                <a asp-area="Management"
                                   asp-controller="ManagerFarmer"
                                   asp-action="Edit"
                                   asp-route-id="@user.userid"
                                   class="btn btn-sm btn-warning me-2">
                                    <i class="fas fa-edit"></i> Sửa
                                </a>

                                @* Thay anchor Delete bằng form POST *@
                                <form asp-area="Management"
                                      asp-controller="ManagerFarmer"
                                      asp-action="Delete"
                                      asp-route-id="@user.userid"
                                      method="post"
                                      class="d-inline"
                                      onsubmit="return confirm('Bạn có chắc muốn xóa?');">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-danger">
                                        <i class="fas fa-trash"></i> Xóa
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center text-muted">Không có người dùng nào.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
